import 'package:flutter/material.dart';

class Page_Error extends StatelessWidget {
  const Page_Error({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("PAGINA NO ESPERADA"),
      ),
    );
  }
}
